# Chart digest

One line per chart in the gallery. Pick two or three whose *when to use* and *data* match
the request, then read those exemplars in full from `exemplars/<id>.tsx`.

Format: `id` · category · route · when to use · data

- `bar` · Basic · jsx · Compares a single value across a small set of named categories, sorted so the ranking reads off the length. · `{ letter: string; frequency: number }[]`
- `line` · Basic · jsx · A single continuous quantity tracked over time when the shape of the trend matters more than individual readings. · `{ date: Date; value: number }[]`
- `area` · Basic · jsx · One time series where accumulated volume or magnitude should feel weighty, not just the trend line itself. · `{ date: Date; value: number }[]`
- `pie` · Basic · jsx · Part of a whole across a handful of categories when the shares, not the precise values, are the point. · `{ name: string; value: number }[]`
- `multi-line` · Basic · jsx · Several time series on one shared axis when you need to compare their shapes and pick one out by hovering. · `{ date: Date; division: string; rate: number }[]`
- `stacked-area` · Basic · jsx · Composition of a total over time when both the running total and each part's contribution matter. · `{ industries: readonly Industry[]; rows: UnemploymentRow[] }`
- `scatterplot-shapes` · Basic · jsx · Correlation between two measures across a few groups when color alone must not carry the grouping, as in print or for colorblind readers. · `{ points: { species: string; sepalLength: number; sepalWidth: number }[]; species: string[]; }`
- `band` · Basic · jsx · A range per date, a min and max or a confidence interval, rather than one value per date. · `{ date: Date; low: number; high: number }[]`
- `difference` · Basic · jsx · Two series measured on the same dates when the gap between them, and which one is ahead, is the story. · `{ date: Date; sanFrancisco: number; newYork: number }[]`
- `normalized-stacked-area` · Basic · jsx · Share of a total over time when relative composition matters and the absolute total should be suppressed. · `{ rows: GenerationRow[]; sources: readonly Source[] }`
- `area-missing` · Basic · jsx · A filled time series with holes in it, drawn so the gaps read as absent data instead of being bridged. · `{ date: Date; close: number | null }[]`
- `line-missing` · Basic · jsx · Same as an ordinary line chart but the series has holes, and the breaks should be visible rather than interpolated across. · `{ date: Date; close: number | null }[]`
- `index-chart` · Basic · jsx · Several series at wildly different absolute levels compared by relative growth from a starting date the reader can move. · `{ date: Date; symbol: string; close: number }[]`
- `connected-scatterplot` · Basic · jsx · Two measures plotted against each other with time as the path through the plot, when the trajectory doubles back on itself. · `{ year: number; miles: number; gas: number }[]`
- `change-line` · Basic · jsx · One series expressed as percent change from a fixed basis, with gains and losses shaded on either side of parity. · `{ closes: { date: Date; close: number }[]; basis: number }`
- `music-format-revenue` · Basic · jsx · Successive categories displacing one another across years, labeled in place on their own bands instead of through a legend. · `{ rows: FormatYear[]; formats: readonly MusicFormat[] }`
- `diverging-bar` · Comparison · jsx · Ranked signed values where growth and decline should read as opposite directions from a zero baseline. · `{ state: string; pop2010: number; pop2019: number }[]`
- `slope` · Comparison · jsx · Exactly two time points across many named items, so each item's rise or fall reads as the direction of its own line. · `{ rows: { country: string; before: number; after: number }[]; columns: [string, string]; }`
- `marimekko` · Comparison · jsx · Two categorical dimensions at once, where column width carries one total and the stacked height carries the split inside it. · `{ name: string; value?: number; children?: Data[] }`
- `inequality-in-cities` · Comparison · jsx · A start and end value per row drawn as an arrow, ranked by the size of the move rather than by either endpoint. · `{ city: string; start: number; end: number }[]`
- `survival-rates` · Comparison · jsx · Several series across a few ordered stages where every value is labeled in place, extending a two-column slope chart to four. · `{ steps: string[]; conditions: { name: string; values: number[] }[]; }`
- `diverging-stacked-bar` · Comparison · jsx · Likert-style agreement scales, where the neutral midpoint sits at zero and disagreement extends to the left. · `{ question: string; 'Strongly disagree': number; Disagree: number; Neutral: number; Agree: number; 'Strongly agree': number; }[]`
- `stacked-horizontal-bar` · Comparison · jsx · Long row labels with an internal breakdown, when the absolute row totals still need to be comparable. · `{ ageGroups: string[]; rows: { state: string; values: number[] }[]; }`
- `stacked-normalized-horizontal-bar` · Comparison · jsx · Composition compared across rows when only the shares matter and the row totals differ too much to compare directly. · `{ ageGroups: readonly AgeGroup[]; rows: StateAgeRow[] }`
- `methods-of-comparison` · Comparison · jsx · Choosing how to encode change between two values, shown as six small maps of difference, relative change and log ratio side by side. · `{ id: string; name: string; a: number; b: number }[]`
- `bollinger` · Time Series · jsx · A price series wrapped in a rolling mean and a volatility envelope computed from a moving standard deviation. · `{ date: Date; close: number }[]`
- `calendar` · Time Series · jsx · Daily values across several years laid out as a weeks-by-weekday grid, so weekday and seasonal patterns surface. · `{ date: Date; value: number }[]`
- `moving-average` · Time Series · jsx · A noisy daily count where the choice of smoothing window is itself the subject, with several windows drawn over the raw series. · `{ date: Date; count: number }[]`
- `temperature-trends` · Time Series · jsx · Many noisy dated readings with a smoothed trend over them, points colored by their sign around a zero reference. · `{ anomalies: { date: Date; value: number }[]; trend: { date: Date; value: number }[]; }`
- `sea-ice-extent` · Time Series · jsx · Many years of one seasonal cycle overlaid on a single day-of-year axis, colored by year so decade-scale drift shows. · `{ year: number; values: { date: Date; value: number }[] }[]`
- `mareys-trains` · Time Series · jsx · Vehicles moving along a fixed route, with time across and distance along the route down, so each line's slope is its speed. · `{ stations: Station[]; trains: { id: string; number: number; type: ServiceType; direction: 'N' | 'S'; stops: Stop[]; }[]; }`
- `us-population-by-state` · Time Series · jsx · Shows how a total grew while its many constituent parts shifted share, with the biggest bands named in place rather than in a legend. · `{ rows: { year: number; byState: Record<string, number> }[]; years: number[]; states: string[]; regions: readonly string[]; regionByState: Map<string, string>; }`
- `line-with-tooltip` · Time Series · jsx · A single time series where the reader needs the exact value at any date, not just the shape. · `{ date: Date; close: number }[]`
- `realtime-horizon` · Time Series · jsx · Live telemetry from many channels at once, scrolling, where vertical space per channel is tiny. · no dataset, procedural
- `world-history-timeline` · Time Series · jsx · Many overlapping intervals with start and end dates, packed into as few rows as their spans allow. · `{ civilizations: { civilization: string; start: number; end: number; region: string }[]; regions: string[]; }`
- `predator-and-prey` · Time Series · jsx · An animated simulation where two coupled quantities cycle, shown as time series beside the phase orbit they trace. · no dataset, procedural
- `nz-tourists` · Time Series · jsx · A long series spanning orders of magnitude, where a linear/log toggle and dated event rules explain the turns. · `{ points: { date: Date; value: number }[]; events: { date: Date; label: string }[]; }`
- `normal-quantile` · Distribution · jsx · Tests whether one sample is normally distributed, by plotting it against the normal scores it would have if it were. · `number[]`
- `hexbin` · Distribution · jsx · A scatterplot so dense that points overplot; hexagonal bins encode count as color instead. · `{ carat: number; price: number }[]`
- `hexbin-area` · Distribution · jsx · Same dense-scatter binning as its color-encoded sibling, but count is encoded as mark size, which reads on any background. · `{ carat: number; price: number }[]`
- `volcano-contours` · Distribution · jsx · A regular grid of measurements where the contour bands themselves are the subject, like elevation or a survey. · `{ values: number[]; columns: number; rows: number }`
- `vector-field` · Distribution · jsx · A field with direction and magnitude at every point, where arrows read better than color alone. · no dataset, procedural
- `voronoi-stippling` · Distribution · jsx · Renders a continuous density field as discrete dots, so tone comes from dot spacing rather than fill. · no dataset, procedural
- `beeswarm-mirrored` · Distribution · jsx · Shows every individual observation's position along one axis without overlap, spread symmetrically about a center line. · `{ name: string; weight: number }[]`
- `voronoi-labels` · Distribution · jsx · Labels a scattered set of named points by giving each the room its Voronoi cell allows, dropping the cramped ones. · `{ x: number; y: number; label: string }[]`
- `kde` · Distribution · jsx · Shows a distribution's shape without the arbitrary bin edges of a histogram, and how much the bandwidth choice matters. · `number[]`
- `qq-plot` · Distribution · jsx · Compares two samples' whole distributions against each other, not just their means, on a square 45-degree frame. · `{ a: number[]; b: number[] }`
- `contours` · Distribution · jsx · Contours a function evaluated on a grid, with log-spaced thresholds because the values span orders of magnitude. · no dataset, procedural
- `density-contours` · Distribution · jsx · Reveals clusters in a scatterplot by estimating 2D density, better than hexbins when the shape of the modes matters. · `{ waiting: number; eruptions: number }[]`
- `hr-diagram` · Distribution · jsx · A dense scatterplot on a deliberately inverted axis, where the population's clusters get named annotations. · `{ id: number; bv: number; magnitude: number; color: string }[]`
- `psr-b1919-21` · Distribution · jsx · Many sequential traces stacked with heavy overlap, so each row's peaks rise over the rows behind it. · `number[][]`
- `impact-of-vaccines` · Heatmap · jsx · A category-by-year matrix where a single intervention date should visibly split the grid before and after. · `{ regions: string[]; years: number[]; incidence: (number | null)[][]; startYear: number; endYear: number; eventYear: number; }`
- `electric-usage` · Heatmap · jsx · A full year of hourly readings as a day-by-hour grid, with a diverging ramp so sign reads as clearly as magnitude. · `{ usage: number[]; days: number; hoursPerDay: number; yearStart: Date; }`
- `population-by-age` · Heatmap · jsx · A year-by-age grid where cohorts read as diagonals, so historic events leave visible streaks across generations. · `{ cells: { year: number; age: number; value: number }[]; firstYear: number; lastYear: number; maxAge: number; }`
- `variable-color-line` · Encoding · jsx · One continuous series whose segments must be colored by a categorical attribute that changes along the line. · `{ observations: { date: Date; temperature: number; condition: string }[]; conditions: { key: string; label: string; color: string }[]; }`
- `gradient-encoding` · Encoding · jsx · One series where the value should be legible from color as well as height, using a continuous ramp along the stroke. · `{ date: Date; temperature: number }[]`
- `threshold-encoding` · Encoding · jsx · One series that should read as above or below a single cutoff, with the line's color flipping hard at that line. · `{ date: Date; temperature: number }[]`
- `styled-axes` · Encoding · jsx · Compares axis treatments side by side on one series, to decide how much axis a chart actually needs. · `{ date: Date; value: number }[]`
- `color-legend` · Encoding · jsx · Builds the legend itself rather than a chart, covering continuous, diverging, threshold and discrete scales. · no dataset, procedural
- `color-schemes` · Encoding · jsx · A reference sheet of the built-in color ramps and palettes, for picking one before building a chart. · no dataset, procedural
- `watercolor` · Encoding · jsx · An illustrative map where hand-drawn wobble and a four-coloring matter more than precise borders or data values. · no dataset, procedural
- `inline-labels` · Annotation · jsx · A multi-series line chart where labels sit on the lines at each column, so no legend lookup is needed. · `{ points: { date: Date; fruit: string; value: number }[]; columns: Date[]; }`
- `direct-labels` · Annotation · jsx · A multi-series line chart where each line is named at its own right end, so the eye never round-trips to a legend. · `{ name: string; values: { year: number; value: number }[] }[]`
- `icicle` · Hierarchy · jsx · Shows a hierarchy's depth and each node's share as nested rows, when a treemap's shuffled rectangles would hide the level structure. · `{ name: string; value?: number; children?: Data[] }`
- `cluster` · Hierarchy · jsx · A taxonomy where every leaf matters equally, so all leaves align on one edge instead of being spaced by depth like a tidy tree. · `{ name: string; children?: Data[] }`
- `indented-tree` · Hierarchy · jsx · A hierarchy read as a list of indented rows with per-node counts and sizes, for trees far deeper than they are wide. · `{ name: string; value?: number; children?: Data[] }`
- `hierarchical-bar` · Hierarchy · jsx · Ranks the children of one node in a value hierarchy as bars on a shared scale, for reading a breakdown one level at a time. · `{ tree: RegionNode; focus: string[] }`
- `nested-treemap` · Hierarchy · jsx · Sizes leaves by value while keeping every intermediate group visible as a labelled frame, unlike a flat treemap that shows only leaves. · `{ name: string; value?: number; children?: Data[] }`
- `cascaded-treemap` · Hierarchy · jsx · A nested treemap where each level overhangs its parent with a drop shadow, so several levels of nesting stay readable. · `{ name: string; value?: number; children?: Data[] }`
- `sequences-sunburst` · Hierarchy · jsx · Traces the ordered paths people take through a funnel, each ring a further step, with a breadcrumb naming the hovered route. · `{ sequences: { path: string; count: number }[]; steps: string[]; }`
- `tree-of-life` · Hierarchy · jsx · A radial tree whose branch lengths carry real distance, for phylogenies and any tree where edge weight means something. · `{ tree: CladeNode; domains: readonly string[] }`
- `bubble-chart` · Hierarchy · jsx · Compares many positive values by circle area when a bar chart would need too much length and the exact ordering does not matter. · `{ modules: { id: string; value: number }[]; groups: string[] }`
- `pack-enclose` · Hierarchy · jsx · Packs a bare list of radii into tangent circles and draws the smallest circle containing them all, with no hierarchy involved. · `number[]`
- `stern-brocot-tree` · Hierarchy · jsx · Lays out a binary tree whose nodes are generated by a rule rather than supplied as data, here every positive rational as a fraction. · no dataset, procedural
- `chord` · Network · jsx · Two-way flows among a small set of groups, where each pair's exchange in both directions has to be read at once. · `{ names: string[]; matrix: number[][] }`
- `directed-chord` · Network · jsx · Flows among groups where direction matters and each ribbon's two ends differ, built from a source-target-value edge list rather than a matrix. · `{ source: string; target: string; value: number }[]`
- `chord-dependency` · Network · jsx · Directed dependencies among many named modules laid out as a ring, when a force graph of the same edges would be a hairball. · `{ source: string; target: string; value: number }[]`
- `edge-bundling` · Network · jsx · Relations between the leaves of a hierarchy, routed through common ancestors so hundreds of edges read as bundles instead of a mesh. · `{ tree: ModuleNode; groups: string[] }`
- `edge-bundling-bilevel` · Network · jsx · Hierarchical edge bundling coloured by whether each node mostly imports or is mostly imported, when the direction of coupling is the question. · `{ name: string; children?: Data[]; imports?: string[] }`
- `chord-labelled` · Network · jsx · A chord diagram carrying a graduated ruler around the ring, for when readers need absolute magnitudes and not just relative arc size. · `{ names: string[]; matrix: number[][] }`
- `force-directed-graph` · Network · ref · A general network with no inherent layout, where community structure should emerge from the link pattern itself. · `{ nodes: { id: string; group: string }[]; links: { source: string; target: string; value: number }[]; groups: string[]; }`
- `disjoint-force-graph` · Network · ref · A network that breaks into separate components, held on screen by x and y forces because one centring force would fling the islands off. · `{ nodes: { id: string; group: number }[]; links: { source: string; target: string; value: number }[]; }`
- `force-directed-tree` · Network · ref · A hierarchy laid out by simulation instead of a tidy tree, when you want organic branching and no fixed depth columns. · `{ tree: TreeNode; packages: string[] }`
- `mobile-patent-suits` · Network · ref · A directed network where edge type and reciprocity matter: every link bows so mutual edges do not overdraw, and arrowheads carry direction. · `{ companies: string[]; suits: { source: string; target: string; type: SuitType }[]; types: SuitType[]; }`
- `temporal-force-graph` · Network · ref · A network whose nodes and links come and go over time, replayed frame by frame so growth and churn are visible. · `{ nodes: { id: string; group: string; start: number; end: number }[]; links: { source: string; target: string; start: number; end: number }[]; frames: number[]; topics: string[]; }`
- `tangled-tree` · Network · jsx · A hierarchy where a node can have more than one parent, drawn as merged bundles by generation instead of a strict tree. · `{ id: string; parents?: string[] }[][]`
- `parallel-coordinates` · Multivariate · jsx · Compares many numeric measures across the same rows to reveal which variables trade off, where a scatterplot shows only two at a time. · `{ rows: Vehicle[]; dimensions: { key: Exclude<keyof Vehicle, 'name'>; label: string; format: string; ticks: number; }[]; }`
- `parallel-sets` · Multivariate · jsx · Follows how records split across several categorical stages, for columns that are categories rather than the numbers parallel coordinates needs. · `{ rows: Outcome[]; dimensions: { key: Exclude<keyof Outcome, 'count'>; label: string }[]; }`
- `splom` · Multivariate · jsx · Every pair of numeric columns plotted at once, to find which pair actually correlates before committing to a single scatterplot. · `{ rows: Penguin[]; dimensions: { key: PenguinDimension; label: string }[]; species: string[]; }`
- `brushable-splom` · Multivariate · ref · A scatterplot matrix where dragging a box in one cell highlights the same records in every other, for chasing a cluster across variable pairs. · `{ rows: Penguin[]; dimensions: { key: PenguinDimension; label: string }[]; species: PenguinSpecies[]; }`
- `radial-stacked-bar` · Radial · jsx · Stacked totals over a cycle that wraps, such as hours of the day, where a straight bar chart would cut the loop open. · `{ rows: Row[]; series: Exclude<keyof Row, 'hour'>[] }`
- `radial-stacked-bar-grouped` · Radial · jsx · The ranked radial variant: bands sorted by total so the largest sit at the top, for categories with no natural order. · `{ rows: Row[]; series: Exclude<keyof Row, 'region'>[] }`
- `radial-area` · Radial · jsx · A year of daily ranges wrapped into a ring, so the seasonal cycle closes instead of running off the right edge of a time axis. · `{ days: { date: Date; avg: number; min: number; max: number; minmin: number; maxmax: number }[]; start: Date; end: Date; }`
- `radial-cluster` · Radial · jsx · Wraps a deep hierarchy into a circle with every leaf on one outer ring, so leaf names read as a single labelled rim. · `{ name: string; children?: Data[] }`
- `radial-tree` · Radial · jsx · Wraps a hierarchy into a circle with each level on its own ring; reach for it over the dendrogram when depth matters more than leaf alignment. · `{ name: string; children?: Data[] }`
- `world-map` · Geo · jsx · A plain reference basemap of the world when you need geography as context rather than as an encoding of any value. · no dataset, procedural
- `world-choropleth` · Geo · jsx · Shades every country by one comparable rate or index, showing where a value is high or low across the globe. · `Map<string, number>`
- `us-state-choropleth` · Geo · jsx · Shades US states by one value per state, for showing where a rate is high or low across the country. · `Map<string, number>`
- `bivariate-choropleth` · Geo · jsx · Maps two variables at once per region to show where they coincide, instead of putting two single-variable choropleths side by side. · `Record<string, { diabetes: number; obesity: number }>`
- `bubble-map` · Geo · jsx · Sizes a circle at each place by an absolute count, which a choropleth cannot show honestly because shading conflates area with magnitude. · `Record<string, number>`
- `spike-map` · Geo · jsx · Encodes magnitude per place as a vertical spike, which stays legible where bubbles would overlap into a blob in dense regions. · `{ id: string; name: string; value: number }[]`
- `non-contiguous-cartogram` · Geo · jsx · Scales each region about its own centroid by value, so small high-value regions read as big without deforming the whole map into one blob. · `Record<string, number>`
- `projection-comparison` · Geo · jsx · Puts eight world projections in one grid so you can pick a projection by seeing what each does to shape and area. · no dataset, procedural
- `tissots-indicatrix` · Geo · jsx · Overlays a lattice of equal-size circles on a projection so its distortion is visible directly, rather than argued about. · no dataset, procedural
- `us-airports-voronoi` · Geo · jsx · Partitions a map into the catchment of the nearest site, answering which facility serves any given point rather than where the sites are. · `Array< [longitude: number, latitude: number, iata: string, name: string, city: string, state: string] >`
- `hexbin-map` · Geo · jsx · Bins thousands of lat/lon points into hexagons when plotting each point would just produce an unreadable overplotted smear. · `{ lon: number; lat: number; year: number }[]`
- `solar-terminator` · Geo · jsx · Overlays a computed spherical cap on a world map to show what is in daylight and what is in darkness at a chosen instant. · no dataset, procedural
- `star-map` · Geo · jsx · Plots points given in celestial coordinates on a polar sky chart, with symbol size carrying brightness rather than a data value. · `{ ra: number; dec: number; magnitude: number; name?: string }[]`
- `spilhaus` · Geo · jsx · Centers the projection on the ocean so the world's waters read as one connected body and land is pushed to the rim. · no dataset, procedural
- `walmarts-growth` · Geo · jsx · Animates dated point locations appearing over time to show how a network spread geographically, not just where it ended up. · `{ stores: { id: string; lon: number; lat: number; date: number }[]; startDate: number; endDate: number; }`
- `antimeridian-cutting` · Geo · jsx · Demonstrates how a continuously rotated projection cuts geometry at the antimeridian instead of streaking it across the frame. · no dataset, procedural
- `graticule-labels` · Geo · jsx · Annotates meridians and parallels with their coordinates around a hemisphere's rim, when readers need to locate positions rather than just see shapes. · no dataset, procedural
- `ortho-to-equirect` · Geo · jsx · Morphs a globe into a flat map by interpolating the raw projections, showing that the two are the same surface unrolled. · no dataset, procedural
- `projection-transitions` · Geo · jsx · Cycles smoothly through a dozen world projections so their differences show up as motion rather than as a static grid. · no dataset, procedural
- `versor-dragging` · Geo · ref · Lets a reader drag a globe to any orientation with the grabbed point staying under the cursor, for exploring geography interactively. · no dataset, procedural
- `world-tour` · Geo · jsx · Flies a globe from country to country along great-circle arcs, for a guided narrated sequence of places rather than an all-at-once map. · `string[]`
- `world-airports-voronoi` · Geo · jsx · Builds Voronoi cells on the sphere itself, which the planar diagram cannot do once sites wrap the whole globe. · `[number, number, string, string, string][]`
- `word-cloud` · Text · jsx · Sizes words by frequency in a space-filling spiral, for a quick impression of a text's vocabulary when exact comparison is not the point. · `{ text: string; count: number }[]`
- `occlusion` · Text · jsx · Keeps a dense scatter labelled by dropping labels that collide, in priority order, instead of letting every label overlap. · `{ id: number; x: number; y: number; label: string; weight: number }[]`
- `centerline-labeling` · Text · jsx · Sets a name along the medial axis inside an irregular shape, so a region's label follows its form instead of sitting at its centroid. · no dataset, procedural
- `bar-chart-race` · Animation · jsx · Animates a ranked bar list through time, when the story is who overtook whom rather than the shape of any single series. · `{ frames: { label: string; values: { name: string; sector: string; value: number; rank: number }[] }[]; sectors: string[]; }`
- `animated-treemap` · Animation · jsx · Holds a treemap's cell positions fixed while the values change, so shifting shares of a whole are readable across time steps. · `{ years: number[]; population: PopulationNode }`
- `scatterplot-tour` · Animation · jsx · Animates a scatter through a sequence of 2D projections of high-dimensional data, revealing structure no single pair of axes shows. · `{ points: { id: number; cls: number; dims: number[] }[]; projections: { name: string; x: number; y: number }[]; dimensionNames: string[]; classNames: string[]; }`
- `wealth-health` · Animation · jsx · Animates a bubble scatter through years, showing how many entities moved through a two-variable space rather than where they ended. · `{ nations: { name: string; region: string; income: number[]; life: number[]; population: number[]; }[]; regions: readonly string[]; years: number[]; }`
- `epicyclic-gearing` · Animation · jsx · Explains how a mechanism moves, drawing its parts from geometry and spinning them continuously; there is no dataset behind it. · no dataset, procedural
- `arc-tween` · Animation · ref · Animates one value changing on a radial gauge, using a custom attrTween because interpolating the arc's path string directly would tear it. · `number[]`
- `polar-clock` · Animation · jsx · Shows progress through several nested time cycles at once as concentric sweeping rings, instead of one progress bar per cycle. · no dataset, procedural
- `tadpoles` · Animation · jsx · Organic particle motion with trailing bodies, when you want a living animated scene rather than an encoding of measurements. · no dataset, procedural
- `phases-of-the-moon` · Astronomy · jsx · A year of daily values as a month-by-day grid where each day is a drawn glyph rather than a coloured square. · no dataset, procedural
- `solar-path` · Astronomy · jsx · Angle-and-elevation data on a circular sky dome, where a cartesian plot would cut the wrap-around geometry apart. · no dataset, procedural
- `zoomable-area` · Interaction · ref · A long dense time series the reader must scrub into: zoom rescales the date axis while the value axis stays fixed. · `{ date: Date; count: number }[]`
- `zoomable-circle-packing` · Interaction · ref · A deep part-of-whole hierarchy explored by clicking down into it, when containment matters more than precise size comparison. · `{ name: string; value?: number; children?: Data[] }`
- `collapsible-tree` · Interaction · ref · A tree too large to show at once, where the reader expands only the branches they care about and the rest stays folded away. · `{ name: string; children?: Data[] }`
- `zoomable-treemap` · Interaction · jsx · Sizes inside a deep hierarchy one level at a time, clicking a cell to descend, rather than showing every leaf at once. · `{ name: string; value?: number; children?: Data[] }`
- `zoomable-icicle` · Interaction · ref · A hierarchy where depth and sibling order must stay legible: one column per level, unlike a treemap's nested boxes. · `{ name: string; value?: number; children?: Data[] }`
- `zoomable-bar-chart` · Interaction · ref · Far more bars than fit across the frame: zoom stretches the band scale until individual bars and their labels become readable. · `{ code: string; count: number }[]`
- `pannable-chart` · Interaction · jsx · A time series far wider than its container, scrolled sideways with the value axis pinned in place instead of zoomed. · `{ date: Date; close: number }[]`
- `zoom-to-bounding-box` · Interaction · ref · A map where clicking a region frames that region, with the zoom target computed from the feature's bounds rather than the pointer. · no dataset, procedural
- `smooth-zooming` · Interaction · ref · Automated camera moves between far-apart points at different scales, where a straight pan plus scale would feel wrong. · no dataset, procedural
- `scatterplot` · Basic · jsx · Relationship between two numeric measures across a modest set of named items, where each point is worth identifying. · `{ name: string; mpg: number; hp: number }[]`
- `donut` · Basic · jsx · Parts of one total across a handful of named categories, with the ring's centre left free for a headline number. · `{ name: string; value: number }[]`
- `horizontal-bar` · Basic · jsx · Ranking one value across named categories whose labels are too long to sit under vertical bars. · `{ letter: string; frequency: number }[]`
- `histogram` · Distribution · jsx · Shape of one numeric variable's distribution: where the values pile up, rather than the individual observations. · `number[]`
- `box-plot` · Distribution · jsx · How one variable's spread shifts across bins of another, summarised as quartiles and outliers instead of every point. · `{ carat: number; price: number }[]`
- `beeswarm` · Distribution · jsx · Every individual observation along a single axis with none hidden by overlap, where a histogram would erase the items themselves. · `{ name: string; weight: number }[]`
- `ridgeline` · Distribution · jsx · How a distribution's shape changes group by group, with rows overlapping so far more series fit than small multiples allow. · `{ name: string; values: number[] }[]`
- `stacked-bar` · Comparison · jsx · Total per category and its internal composition at once, when the totals matter as much as the parts do. · `{ sources: string[]; rows: { country: string; values: number[] }[]; }`
- `grouped-bar` · Comparison · jsx · Comparing sub-categories directly against each other inside groups, where stacking would bury the individual values. · `{ rows: { state: string; age: string; population: number }[]; states: string[]; ages: string[]; }`
- `dot-plot` · Comparison · jsx · The spread between several series within each row, as dots on a range line, replacing a grouped bar chart when only the gaps matter. · `{ ages: readonly string[]; shares: { state: string; age: string; share: number }[]; }`
- `candlestick` · Time Series · jsx · Open, high, low and close per period, where a line of closing prices alone would throw away the range inside each period. · `{ date: Date; open: number; high: number; low: number; close: number }[]`
- `horizon` · Time Series · jsx · Dozens of parallel time series compared at a glance when each can be given only a few pixels of vertical space. · `{ name: string; values: { date: Date; value: number }[] }[]`
- `streamgraph` · Time Series · jsx · Composition shifting over time when the flowing shape matters more than reading exact totals off a flat baseline. · `{ rows: UnemploymentRow[]; industries: readonly Industry[] }`
- `treemap` · Hierarchy · jsx · Every leaf of a hierarchy at once, sized by value, when the reader should scan all the items rather than drill into one. · `{ name: string; value?: number; children?: Data[] }`
- `sunburst` · Hierarchy · jsx · A hierarchy's full depth radially, where the root fills the circle and each outward ring is one level down. · `{ name: string; value?: number; children?: Data[] }`
- `tree` · Hierarchy · jsx · Parent-child structure where the branching itself is the message and no node carries a size to encode. · `{ name: string; children?: Data[] }`
- `pack` · Hierarchy · jsx · Nested groups where the leaf sizes are the point and you want every group readable as a shape, not a treemap's rectangles. · `{ name: string; value?: number; children?: Data[] }`
- `sankey` · Network · jsx · A conserved quantity moving through stages, where the width of each flow and where it splits or is lost is the story. · `{ nodes: { id: string; category: SankeyCategory }[]; links: { source: string; target: string; value: number }[]; }`
- `arc-diagram` · Network · jsx · Links among items whose ordering carries meaning, when labels must stay legible and a force layout would scramble that order. · `{ nodes: { id: string; year: number; group: string }[]; links: { source: string; target: string }[]; }`
- `choropleth` · Geo · jsx · One value per named region on a map, binned into a handful of shaded classes rather than a continuous ramp. · `{ rates: Map<string, number>; domain: [number, number] }`
- `bar-transitions` · Animation · jsx · Re-sorting the same bars so a reader tracks individual categories across the reorder instead of reading three separate charts. · `{ letter: string; frequency: number }[]`
- `stacked-to-grouped` · Animation · jsx · Showing that a stack and a grouped set of bars are the same numbers, when the reader needs both the total and the per-series comparison. · `number[][]`
- `pie-update` · Animation · ref · Switching a donut between two measures of the same slices, when the change in each slice's share is what matters. · `{ rows: { apples: number; oranges: number }[]; keys: ('apples' | 'oranges')[]; }`
- `streamgraph-transitions` · Animation · ref · Demonstrating how stack offset and order reshape the same layered series, from a wiggly stream to a straight 100% stack. · `number[][][]`
- `brushable-scatterplot` · Interaction · ref · Two measurements per row where the reader needs to select a region of the cloud and see how many points fall inside it. · `{ mpg: number; hp: number }[]`
- `zoomable-sunburst` · Interaction · ref · A hierarchy too deep to show at once, when drilling into a branch and keeping the radial context beats a static sunburst. · `{ name: string; value?: number; children?: Data[] }`
- `uniform-sampling` · Algorithms · jsx · Showing why independent uniform random points are a poor sample: the clumps and gaps are visible as the points land. · no dataset, procedural
- `best-candidate-sampling` · Algorithms · jsx · Explaining Mitchell's best-candidate: each new point is the one of several random tries whose nearest neighbour is farthest away. · no dataset, procedural
- `poisson-disc-sampling` · Algorithms · jsx · Explaining Bridson's algorithm, where a background grid makes a minimum-distance point set cheap to generate. · no dataset, procedural
- `sampling-voronoi` · Algorithms · jsx · Comparing sampling methods quantitatively rather than by eye, by colouring each point's Voronoi cell by area on one shared scale. · no dataset, procedural
- `quicksort` · Algorithms · jsx · Showing quicksort as it runs, when the reader should see the active partition narrowing and the pivot doing the work. · no dataset, procedural
- `quicksort-threads` · Algorithms · jsx · Showing a whole sort at once instead of animating it: time runs down the page and every swap is a visible braid. · no dataset, procedural
- `mergesort` · Algorithms · jsx · Showing a bottom-up merge sort, where each completed pass is kept below as a small multiple so the progress toward order is legible. · no dataset, procedural
- `maze-random-traversal` · Algorithms · jsx · Showing the plainest spanning-tree maze generator, taking a uniformly random edge off the frontier, as a baseline for the other three. · no dataset, procedural
- `maze-depth-first` · Algorithms · jsx · Showing why depth-first carving produces long winding corridors and deep dead ends, unlike the frontier-based generators. · no dataset, procedural
- `maze-prims` · Algorithms · jsx · Showing Prim's growing outward from a single seed by lowest-weight frontier edge, giving a bushy maze with short dead ends. · no dataset, procedural
- `maze-wilsons` · Algorithms · jsx · Showing Wilson's loop-erased random walk, the one generator that samples spanning trees uniformly and so has no structural bias. · no dataset, procedural
- `maze-flood-fill` · Algorithms · jsx · Colouring a graph by distance from an origin, when the reader should see the wavefront sweep outward rather than a static heat map. · no dataset, procedural
- `maze-tree` · Algorithms · jsx · Re-encoding the same structure two ways to prove a claim, here that a maze is literally a spanning tree once you straighten it out. · no dataset, procedural
- `fisher-yates-shuffle` · Algorithms · jsx · Teaching the Fisher-Yates invariant, that everything from i rightwards is already final, by shading that region as it grows. · no dataset, procedural
- `shuffle-bias` · Algorithms · jsx · Proving an algorithm is biased empirically: two heatmaps of where each element lands over many trials, on one shared colour scale. · no dataset, procedural
- `quadtree` · Algorithms · jsx · Showing why a spatial index is fast: which cells a nearest-neighbour search actually visited and which whole subtrees it threw away. · no dataset, procedural
- `maze-kruskals` · Algorithms · jsx · Showing Kruskal's committing the cheapest edges in global order, so scattered fragments appear everywhere at once and fuse into one spanning tree — the contrast to Prim's single growing blob. · no dataset, procedural
- `pathfinding-compared` · Algorithms · jsx · Racing breadth-first, Dijkstra, greedy best-first and A* on one shared weighted terrain, when the reader should see each frontier's shape — the wave, the blinkered tongue, the ellipse — and the cells-expanded scoreboard. · no dataset, procedural
- `string-search` · Algorithms · jsx · Racing naive and Boyer-Moore over the same text one comparison per tick, when the point is what is never read — letters gray out as they are examined and Boyer-Moore's ribbon stays mostly bright. · no dataset, procedural
- `wave-function-collapse` · Algorithms · jsx · Showing constraint propagation assembling a pipe circuit: the least-certain cell collapses, constraints ripple outward, and an entropy heat map drains until the grid is decided. · no dataset, procedural
- `bloom-filter` · Algorithms · jsx · Showing a Bloom filter earning its space savings: inserts light three hashed bits in a 256-bit grid, true negatives bounce off a dark bit, and the reader witnesses the first false positive — a word never inserted whose bits were all set by others. · no dataset, procedural
- `radix-sort` · Algorithms · jsx · Showing order emerging from classification instead of comparison: 24 three-digit base-4 values drawn as stacked digit-color bands settle digit by digit, positions tweening as each pass groups by one digit and the comparison counter stays at zero. · no dataset, procedural
- `dp-wavefront` · Algorithms · jsx · Showing dynamic programming as a wavefront: the INTENTION-to-EXECUTION edit distance matrix fills along anti-diagonals, then the traceback lights backwards from the corner and the optimal alignment materializes above it. · no dataset, procedural
- `sieve` · Algorithms · jsx · Showing the Sieve of Eratosthenes striking composites in layered translucent colors, one sweep per prime up to √300, so primes are what is left blank — and the reader sees why stopping at the square root is legal. · no dataset, procedural
- `lloyd-relaxation` · Algorithms · jsx · Showing iterative convergence decelerating: every Voronoi site jumps to its cell centroid, and the displacement whiskers shrink from early leaps to late trembles as the tessellation settles, with max movement captioned per iteration. · no dataset, procedural
- `k-means` · Algorithms · jsx · Showing k-means alternate its assign and update phases on gaussian blobs: boundary points flicker between cluster colors for several rounds before equilibrium, with the count of switched points captioned each round. · no dataset, procedural
- `optimizer-race` · Algorithms · jsx · Racing SGD, momentum and Adam down one two-basin loss surface when the reader should see optimizer temperament: SGD zigzags under noise, momentum overshoots the valley and swings back, Adam hugs the slope, each captioned with its current loss. · no dataset, procedural
- `union-find` · Algorithms · jsx · Making amortized analysis physical: naive unions chain a forest into tall trees, then one path-compressing find drags every walked node directly under the root and the tree height visibly collapses, with depth before and after captioned. · no dataset, procedural
- `huffman-coding` · Algorithms · jsx · Showing greed on the two lightest trees building the provably optimal prefix code: the forest merges bottom-up, then the sentence encodes live with the variable-length bit count racing fixed-width. · no dataset, procedural
- `reservoir-sampling` · Algorithms · jsx · Showing a stream flowing past a fixed-size reservoir where each later arrival is kept with shrinking probability k/i, yet every element ends equally likely — the fading arrival opacity is the odds. · no dataset, procedural
- `skip-list` · Algorithms · jsx · Showing randomized towers buying binary-search speed in a linked list: one search descends level by level and the dim never-visited majority is the payoff — the skipping-work encoding applied to a structure. · no dataset, procedural
- `consistent-hashing` · Algorithms · jsx · Showing keys and nodes on a hash ring through scripted joins and leaves: the moved keys flash and arc to their new owner while the unmoved majority stays put, and the moved-versus-total counter is the argument. · no dataset, procedural
